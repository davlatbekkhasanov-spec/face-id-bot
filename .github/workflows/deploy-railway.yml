name: Deploy Railway

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      YORDAMCHI_HUB_SECRET: ${{ secrets.YORDAMCHI_HUB_SECRET }}
      DEPLOY_HOOK: ${{ secrets.RAILWAY_DEPLOY_HOOK }}
    steps:
      - uses: actions/checkout@v4

      - name: Railway deploy (env + latest commit)
        if: env.RAILWAY_TOKEN != ''
        run: node scripts/railway-deploy.mjs

      - name: Deploy Hook fallback
        if: env.RAILWAY_TOKEN == '' && env.DEPLOY_HOOK != ''
        run: curl -fsS -X POST "$DEPLOY_HOOK"

      - name: Deploy sozlanmagan
        if: env.RAILWAY_TOKEN == '' && env.DEPLOY_HOOK == ''
        run: |
          echo "::error::GitHub Secrets: RAILWAY_TOKEN yoki RAILWAY_DEPLOY_HOOK qo'shing"
          echo "Railway → FaceIdBot → Settings → Deploy Hook → GitHub secret RAILWAY_DEPLOY_HOOK"
          exit 1
