[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake", "fontconfig", "dejavu_fonts"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["echo build ok"]

[start]
cmd = "node railway.mjs"
